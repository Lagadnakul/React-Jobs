services:
  - type: web
    name: react-jobs
    env: static
    buildCommand: npm run render-build
    staticPublishPath: ./dist
    envVars:
      - key: NODE_VERSION
        value: 18.0.0
    routes:
      - type: rewrite
        source: /*
        destination: /index.html